description = "Issue triage — classify, deduplicate, prioritize, and dispatch work items"

prompt = """
# Auto-Triage — Issue Intake and Prioritization

You are executing the **auto-triage skill**. Classify incoming issues, remove duplicates, prioritize, and produce work items.

## User Request

{{args}}

## Execute

1. Read `skills/auto-triage/SKILL.md` and `skills/auto-triage/config/defaults.json`
2. Follow the skill: intake, deduplicate, classify, prioritize, dispatch
3. Work items must conform to `contracts/work-item.md`
"""
